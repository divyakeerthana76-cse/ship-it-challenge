// Ship It Challenge - Jenkinsfile Template
//
// YOUR TASK: Create a Jenkinsfile for the Ship It challenge app
//
// Requirements:
// - 4 stages: Build, Test, Image, Deploy
// - Build stage: npm install or npm ci
// - Test stage: npm test (tests MUST pass - no || true!)
// - Image stage: docker build
// - Deploy stage: docker run with SHIP_TOKEN environment variable
//
// DO NOT copy this file directly - create your own from scratch!
//
// Ask mentors about Jenkins credentials for SHIP_TOKEN
//
// Example structure (fill in the details yourself):
//
// pipeline {
//     agent any
//     environment {
//         IMAGE_NAME = "ship-it"
//         PORT = "3004"
//     }
//     stages {
//         stage('Build') {
//             steps {
//                 sh 'npm ci'
//             }
//         }
//         stage('Test') {
//             steps {
//                 sh 'npm test'
//             }
//         }
//         stage('Image') {
//             steps {
//                 sh "docker build -t ${IMAGE_NAME}:\${BUILD_NUMBER} ."
//             }
//         }
//         stage('Deploy') {
//             steps {
//                 sh """
//                     docker rm -f ship-it 2>/dev/null || true
//                     docker run -d --name ship-it -p \${PORT}:\${PORT} -e SHIP_TOKEN=\${SHIP_TOKEN} \${IMAGE_NAME}:\${BUILD_NUMBER}
//                 """
//             }
//         }
//     }
// }
